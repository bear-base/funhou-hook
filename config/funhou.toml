[[rules]]
match = "Read|Glob|Grep"
level = "info"

[[rules]]
match = "Notification(idle_prompt)"
level = "warning"

[[rules]]
match = "Notification(permission_prompt)"
level = "danger"

[[rules]]
match = "Edit(*.env*)"
level = "danger"

[[rules]]
match = "Bash(*deploy*|*migrate*)"
level = "danger"

[[rules]]
match = "Bash(*test*|*lint*)"
level = "info"

[defaults]
level = "warning"

[channels.terminal]
output = "/tmp/funhou.log"
levels = ["info", "warning", "danger", "error"]